# CLAUDE.md — {{PROJECT_NAME}}

@AGENTS.md

## Claude-specific notes

Shared rules live in `AGENTS.md` and are imported above. Add anything here that
applies only to Claude Code (skills, hooks, `.claude/settings.json` notes).

- Project-scoped skills go in `.claude/skills/<name>/SKILL.md`.
- Do NOT add a `Co-authored-by: Claude` trailer to commit messages.
